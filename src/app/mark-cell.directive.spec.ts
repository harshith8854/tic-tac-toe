import { MarkCellDirective } from './mark-cell.directive';

describe('MarkCellDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkCellDirective();
    expect(directive).toBeTruthy();
  });
});
